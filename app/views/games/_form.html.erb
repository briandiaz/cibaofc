<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.datetime_select :date %>
  </div>
  <div class="field">
    <%= f.label :round_id %><br>
    <%= f.text_field :round_id %>
  </div>
  <div class="field">
    <%= f.label :home_team %><br>
    <%= f.number_field :home_team %>
  </div>
  <div class="field">
    <%= f.label :away_team %><br>
    <%= f.number_field :away_team %>
  </div>
  <div class="field">
    <%= f.label :stadium_id %><br>
    <%= f.text_field :stadium_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
